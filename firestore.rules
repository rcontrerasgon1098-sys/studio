
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // ✅ REGLAS TOTALMENTE ABIERTAS (Solo para pruebas, como solicitaste)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
