rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitimos acceso total a cualquier usuario autenticado para facilitar la operatividad inmediata
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}