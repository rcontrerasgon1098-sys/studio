rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitimos acceso total a cualquier usuario autenticado
    // Esto desactiva las restricciones de rol de administrador que causaban errores
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}