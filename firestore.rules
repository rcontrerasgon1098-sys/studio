rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * ICSA WORKFLOW SECURITY RULES
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    match /users/{userId} {
      allow get: if isSignedIn();
      allow list: if isAdmin();
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if isAdmin();
    }

    // Regla para permitir a los administradores listar todas las órdenes de trabajo mediante consultas de grupo
    match /{path=**}/work_orders/{workOrderId} {
      allow read: if isAdmin();
    }

    match /users/{userId}/work_orders/{workOrderId} {
      allow get, list: if isOwner(userId) || isAdmin();
      
      allow create: if isOwner(userId) 
                    && request.resource.data.technicianId == userId;
      
      allow update: if (isOwner(userId) || isAdmin())
                    && request.resource.data.technicianId == resource.data.technicianId;
      
      allow delete: if isOwner(userId) || isAdmin();
    }

    match /clients/{clientId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isSignedIn(); // Permitir a técnicos crear clientes sobre la marcha
    }

    match /roles_admin/{userId} {
      allow get: if isSignedIn();
      allow list: if isAdmin();
      allow create: if isOwner(userId); // Permitir creación por el dueño para habilitar el setup demo
      allow update, delete: if false; 
    }
  }
}