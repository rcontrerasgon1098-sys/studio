
{
  "entities": {
    "WorkOrder": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkOrder",
      "type": "object",
      "description": "Represents a work order in the Teleconnect Workflow application.",
      "properties": {
        "id": { "type": "string" },
        "folio": { "type": "number" },
        "clientName": { "type": "string" },
        "clientId": { "type": "string" },
        "createdBy": { "type": "string", "description": "FK to Personnel. UID of the creator." },
        "technicianId": { "type": "string", "description": "FK to Personnel. Primary technician." },
        "teamIds": { 
          "type": "array", 
          "items": { "type": "string" },
          "description": "Denormalized array of Personnel IDs for fast querying (Membership)."
        },
        "team": { 
          "type": "array", 
          "items": { "type": "string" },
          "description": "Denormalized array of Personnel names for display."
        },
        "status": { "type": "string", "enum": ["Pending", "Active", "Pending Signature", "Completed"] },
        "startDate": { "type": "string", "format": "date-time" },
        "address": { "type": "string" },
        "building": { "type": "string" },
        "floor": { "type": "string" },
        "description": { "type": "string" },
        "techName": { "type": "string" },
        "techRut": { "type": "string" },
        "techSignatureUrl": { "type": "string", "format": "uri" },
        "clientReceiverName": { "type": "string" },
        "clientReceiverRut": { "type": "string" },
        "clientSignatureUrl": { "type": "string", "format": "uri" }
      },
      "required": ["id", "folio", "clientName", "createdBy", "status"]
    },
    "Client": {
      "title": "Client",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nombreCliente": { "type": "string" },
        "rutCliente": { "type": "string" },
        "emailClientes": { "type": "string" }
      },
      "required": ["nombreCliente", "rutCliente"]
    },
    "Personnel": {
      "title": "Personnel",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nombre_t": { "type": "string" },
        "rol_t": { "type": "string", "enum": ["admin", "supervisor", "tecnico"] },
        "email_t": { "type": "string" }
      },
      "required": ["id", "nombre_t", "rol_t"]
    },
    "TeamAssignment": {
      "title": "TeamAssignment",
      "type": "object",
      "description": "Logical Join Table between WorkOrder and Personnel (Normalization).",
      "properties": {
        "orderId": { "type": "string", "description": "Ref to WorkOrder" },
        "personnelId": { "type": "string", "description": "Ref to Personnel" },
        "role": { "type": "string", "description": "Role in this specific order" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/ordenes/{orderId}",
        "definition": {
          "entityName": "WorkOrder",
          "schema": { "$ref": "#/backend/entities/WorkOrder" },
          "description": "Active work orders."
        }
      },
      {
        "path": "/historial/{orderId}",
        "definition": {
          "entityName": "WorkOrder",
          "schema": { "$ref": "#/backend/entities/WorkOrder" },
          "description": "Completed work orders."
        }
      },
      {
        "path": "/clients/{clientId}",
        "definition": {
          "entityName": "Client",
          "schema": { "$ref": "#/backend/entities/Client" },
          "description": "Client registry."
        }
      },
      {
        "path": "/personnel/{userId}",
        "definition": {
          "entityName": "Personnel",
          "schema": { "$ref": "#/backend/entities/Personnel" },
          "description": "User profiles and roles."
        }
      }
    ]
  }
}
